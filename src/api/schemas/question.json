{
  "prefix": { "type": "string" },
  "metric": { "type": "string" },
  "subject": { "type": "string" },

  "tags": { "type": "array" },
  "isRegional": { "type": "boolean" },
  "isTemporal": { "type": "boolean" },

  "publicity": { "type": "string", "enum": ["private", "shared-unlisted", "shared", "public"] },

  "questionId": { "type": "string", "format": "uuid" }
}
